# Frontend environment variables (Vite)
# Copy to .env and adjust as needed.

# Base URL for the backend API
# When deploying, set this to your backend URL (https://your-backend.example.com)
# For local development, ensure it matches the backend PORT (default 5001)
VITE_API_BASE_URL=http://localhost:5001
